const ComponentRules=()=>{

    //  component--> reusable building block (reusable code)

    // types of components----> 2 ttypes

            // 1.CBC (Class Based Component )------->class keyword❌
            // 1.FBC (Function Based Component)------->Arrow function ✅

            // for emoji: window+ dot(.)

    // Ruules to create a FBC:

        //  1. Component name ( function name ) name should start with capital letter

        //  2. File name and componenent name need to be same ( recommend )

        //  3. Componenets are called in two ways i.e. ----> self-closing tag and paired tag  (recommended one:--->self-closing tag)


    // Q.  Where js need to be write :
          // A. Before the return statement  and inside the component   

    // Q.  Where html( jsx ) need to write

        // A. inside the return statement
        
    // NOTE:

        // inside the return statement / (jsx expression) it is not possible to declare the js variables but we can access it.

    return(
        <div>{/*ComponetRules*/}</div>
    )
}
export default ComponentRules